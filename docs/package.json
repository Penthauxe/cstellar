{
  "name": "@sct01/docs",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vocs dev",
    "build": "vocs build",
    "vercel-build": "npx playwright install --with-deps chromium && npm run build",
    "preview": "vocs preview"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "vocs": "1.0.16"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
